version: '3.8'

services:
  emby-scanner:
    build: .
    container_name: emby-scanner
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - APP_PORT=5000
      - TZ=Europe/Warsaw
    volumes:
      - ./config:/app/config